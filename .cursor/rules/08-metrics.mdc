---
description: Observability (metrics, tracing, logs), naming, cardinality guards, exports.
globs:
  - "**/*metrics*.*"
  - "**/*telemetry*.*"
  - "**/*tracing*.*"
  - "crates/**/src/**/*.{rs}"
  - "runtimes/**/src/**/*.{rs}"
  - "console/**/*.{ts,tsx}"
  - "docs/**/observability*.*"
alwaysApply: false
---


- `tracing` JSON logs: ts, level, msg, function, version, req_id, container_id, duration_ms, billed_ms, mem_peak_mb
- Prometheus:
  - counters: invocations_total{function}, errors_total{function,type}, cold_starts_total{function}
  - histograms: duration_ms{function}, init_duration_ms{function}
- `/metrics` on user API port + health `/healthz`