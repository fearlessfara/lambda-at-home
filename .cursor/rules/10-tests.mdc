---
description: Tests & fixtures
alwaysApply: true
---

- Unit tests per crate; property tests (`proptest`) for scheduler & idle timers.
- Integration (requires Docker): feature `docker_tests`; spawn daemon on ephemeral ports; run create→invoke→stop paths.
- Golden JSON fixtures for Invoke request/response.
- Deterministic timeouts with mock clock in control crate.---
description:
globs:
alwaysApply: false
---
